
int main(int argc, char* argv[])
{
    /**
     *  Операторы break и continue изменяют естественное поведение циклов.
     *
     *  break    - останавливает выполнение цикла в котором находится
     *  continue - прерывает выполнение цикла, пропуская все операторы,
     *             стоящие после него и переходит в начало цикла. В цикле for,
     *             выполняется 3 часть цикла с изменением переменных.
     */
    return 0;
}
